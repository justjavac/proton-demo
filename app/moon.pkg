import {
  "moonbitlang/async",
  "justjavac/proton",
  "justjavac/proton_demo/extensions/counter" @counter,
}

supported_targets = "native"

pkgtype(kind: "executable")

options(
  targets: { "main.mbt": [ "native" ] },
)
